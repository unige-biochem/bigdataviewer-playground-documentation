[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "BigDataViewer Playground"
authors = [{name = "Nicolas Chiaruttini", email = "nicolas.chiaruttini@gmail.com"}]
dynamic = ["version", "description"]
